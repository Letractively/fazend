#summary Simple way to build mock-ups
#labels Phase-Requirements

= Usage Sample =

Inside your view scripts you should use meta-tags to inform FaZend about the functional layout of your pages, e.g.:

{{{
<!-- @actor("RegisteredUser") -->

<!-- @text("You can edit your photos...") -->

<!-- @table()
    ->setColumn("id")
    ->setColumn("description")
    ->setColumnFormat("id", "%3d")
    ->setColumnFormat("description", "%100s")
    ->setOption("disable")
    ->setOption("enable") -->

<!-- @link("Upload new photo", "photos/upload") -->

<!-- @form()
    ->setField("text", "Title of your photo:")
    ->setField("textarea", "Description") -->
}}}