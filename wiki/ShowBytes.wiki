#summary ShowBytes view helper
#labels Phase-Design

= Example usage of ShowBytes view helper =

This is a view script:

{{{
<p>Size of the file is <?=$this->showBytes($size)?>.</p>
}}}

This is a class in model:

{{{
$sizeStr = FaZend_View_Helper_ShowBytes::show($size);
}}}